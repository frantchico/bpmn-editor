/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you plan to output a standalone server, uncomment and configure this:
  // output: 'standalone',
};

export default nextConfig;
